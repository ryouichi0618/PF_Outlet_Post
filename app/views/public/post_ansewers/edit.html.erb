<h1>回答編集画面です</h1>
<%= form_with(model: [@post, @ansewer], url: post_post_ansewer_path(@post, @ansewer), local: true) do |f| %>
  <%= f.text_area :body, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>